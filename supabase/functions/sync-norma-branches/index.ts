const SERVICE = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') || '';
const CRON = Deno.env.get('CRON_SECRET') || '';
const LOCATOR_URL = 'https://www.norma-online.de/cz/filialfinder/';
const UA = 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/150 Safari/537.36';
const CORS = {
  'access-control-allow-origin': '*',
  'access-control-allow-headers': 'authorization,apikey,content-type,x-cron-secret',
  'access-control-allow-methods': 'POST,OPTIONS',
  'content-type': 'application/json; charset=utf-8',
};
function json(body: unknown, status = 200) { return new Response(JSON.stringify(body), { status, headers: CORS }); }
function allowed(request: Request) { const token=(request.headers.get('authorization')||'').replace(/^Bearer\s+/i,'').trim(); if(token&&token===SERVICE)return true; return !!CRON&&request.headers.get('x-cron-secret')===CRON; }
function snippets(html:string,needle:string,max=3){const lower=html.toLowerCase(),search=needle.toLowerCase(),out:string[]=[];let pos=0;while(out.length<max){const found=lower.indexOf(search,pos);if(found<0)break;out.push(html.slice(Math.max(0,found-450),Math.min(html.length,found+1000)).replace(/\s+/g,' '));pos=found+search.length;}return out;}
function cookieHeader(setCookie:string|null){if(!setCookie)return '';return setCookie.split(/,(?=[^;,]+=)/).map(part=>part.trim().split(';')[0]).filter(Boolean).join('; ');}
function mergeCookie(current:string,next:string){const values=new Map<string,string>();for(const source of [current,next])for(const part of source.split(';').map(v=>v.trim()).filter(Boolean)){const eq=part.indexOf('=');if(eq>0)values.set(part.slice(0,eq),part.slice(eq+1));}return [...values].map(([k,v])=>`${k}=${v}`).join('; ');}
Deno.serve(async(request)=>{if(request.method==='OPTIONS')return new Response('ok',{headers:CORS});if(request.method!=='POST')return json({error:'Method not allowed'},405);if(!allowed(request))return json({error:'Unauthorized'},401);try{const input=await request.json().catch(()=>({}));if(input.dry_run!==true||input.mode!=='diagnose')return json({error:'NORMA diagnostic requires dry_run=true and mode=diagnose.'},409);const params=new URLSearchParams();params.set('filialfinder[suche][land]','Tschechien');params.set('filialfinder[suche][radius]',String(input.radius||'10000'));params.set('filialfinder[suche][plz]',String(input.postal_code||''));params.set('filialfinder[suche][stadt]',String(input.city||'Praha'));params.set('filialfinder[suche][strasse]',String(input.street||''));const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),30000);try{const baseHeaders:Record<string,string>={'user-agent':UA,accept:'text/html,application/xhtml+xml,*/*;q=0.8','accept-language':'cs-CZ,cs;q=0.9,en;q=0.7','cache-control':'no-cache'};const init=await fetch(LOCATOR_URL,{method:'GET',headers:baseHeaders,redirect:'manual',signal:controller.signal});let cookie=cookieHeader(init.headers.get('set-cookie'));const chain:Array<{step:string;status:number;url:string;location:string|null;cookie_set:boolean}>=[{step:'init-get',status:init.status,url:init.url,location:init.headers.get('location'),cookie_set:!!cookie}];await init.text();const first=await fetch(LOCATOR_URL,{method:'POST',headers:{...baseHeaders,'content-type':'application/x-www-form-urlencoded',...(cookie?{cookie}:{})},body:params.toString(),redirect:'manual',signal:controller.signal});const postCookie=cookieHeader(first.headers.get('set-cookie'));if(postCookie)cookie=mergeCookie(cookie,postCookie);chain.push({step:'search-post',status:first.status,url:first.url,location:first.headers.get('location'),cookie_set:!!postCookie});let current=first;let html='';for(let hop=0;hop<5;hop+=1){const location=current.headers.get('location');if(!location||![301,302,303,307,308].includes(current.status)){html=await current.text();break;}const target=new URL(location,current.url||LOCATOR_URL).toString();current=await fetch(target,{method:'GET',headers:{...baseHeaders,...(cookie?{cookie}:{})},redirect:'manual',signal:controller.signal});const nextCookie=cookieHeader(current.headers.get('set-cookie'));if(nextCookie)cookie=mergeCookie(cookie,nextCookie);chain.push({step:`redirect-${hop+1}`,status:current.status,url:current.url,location:current.headers.get('location'),cookie_set:!!nextCookie});}if(!html)html=await current.text();const lower=html.toLowerCase();const markerNames=['latitude','longitude','data-lat','data-lng','maps.google','maps.app','openstreetmap','leaflet','filiale','entfernung','öffnungszeiten','google.com/maps'];const markers=Object.fromEntries(markerNames.map(name=>[name,lower.split(name.toLowerCase()).length-1]));const coordinateMatches=[...html.matchAll(/(?:48|49|50|51)[.,][0-9]{3,}[^0-9-]{0,80}(?:12|13|14|15|16|17|18|19)[.,][0-9]{3,}/g)].slice(0,50).map(m=>m[0]);return json({ok:current.ok,dry_run:true,mode:'diagnose',status:current.status,final_url:current.url,bytes:html.length,redirect_chain:chain,markers,coordinate_matches:coordinateMatches,samples:{latitude:snippets(html,'latitude'),longitude:snippets(html,'longitude'),maps:snippets(html,'maps'),result:snippets(html,'entfernung'),hours:snippets(html,'Öffnungszeiten'),praha:snippets(html,'Praha')}},current.ok?200:502);}finally{clearTimeout(timer);}}catch(error){return json({error:error instanceof Error?error.message:String(error),code:'NORMA_DIAGNOSTIC_FAILED'},500);}});
