import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const SUPABASE_URL=Deno.env.get('SUPABASE_URL')!;const SERVICE_ROLE_KEY=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;const CRON_SECRET=Deno.env.get('CRON_SECRET')||'';const db=createClient(SUPABASE_URL,SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});
const CORS={'access-control-allow-origin':'*','access-control-allow-headers':'authorization,apikey,content-type,x-client-info,x-cron-secret','access-control-allow-methods':'POST,OPTIONS','content-type':'application/json; charset=utf-8'};const ACTIVE_IMPORT_STATUSES=['published','review','processing','queued','downloading','publishing'];const SPECIALIZED_CONCURRENCY=4;
const SPECIALIZED:Record<string,string>={action:'sync-action-source',albert:'sync-albert-source',bauhaus:'sync-bauhaus-source',benu:'sync-benu-source',coop:'sync-coop-source',dm:'sync-dm-source','dr-max':'sync-dr-max-source',hruska:'sync-hruska-source',jip:'sync-jip-source',jysk:'sync-jysk-source',kaufland:'verify-scheduled-kaufland-sync',kik:'sync-kik-source',obi:'sync-obi-source',pepco:'sync-pepco-source',rossmann:'sync-rossmann-source',terno:'sync-terno-source',tesco:'sync-tesco-current',teta:'sync-teta-source'};
const PRODUCT_REQUIRED=new Set(['action','benu','billa','coop','dm','flop','globus','hruska','lidl','norma','penny','pepco','rossmann','terno','teta']);
function json(body:unknown,status=200){return new Response(JSON.stringify(body),{status,headers:CORS});}function formatError(error:unknown){if(error instanceof Error)return error.message;if(error&&typeof error==='object'){const value=error as Record<string,unknown>;const parts=[value.message,value.details,value.hint,value.code].filter(Boolean).map(String);if(parts.length)return parts.join(' | ');try{return JSON.stringify(error);}catch{return String(error);}}return String(error);}type AuthorizationResult={ok:true;actor:string}|{ok:false;status:number;error:string};type PipelineResult={store?:string;stores?:string[];verified_stores?:string[];adapter:string;ok:boolean;current_imports?:number;current_offers?:number;current_documents?:number;result?:unknown;error?:string};
async function authorize(request:Request):Promise<AuthorizationResult>{const authorization=request.headers.get('authorization')||'';const cronHeader=request.headers.get('x-cron-secret')||'';if(authorization===`Bearer ${SERVICE_ROLE_KEY}`)return{ok:true,actor:'service-role'};if(CRON_SECRET&&cronHeader===CRON_SECRET)return{ok:true,actor:'cron'};const token=authorization.replace(/^Bearer\s+/i,'').trim();if(!token)return{ok:false,status:401,error:'Unauthorized'};const{data,error}=await db.auth.getUser(token);if(error||!data.user)return{ok:false,status:401,error:'Přihlášení vypršelo. Přihlaste se znovu.'};const role=String(data.user.app_metadata?.role||'').trim().toLowerCase();if(!['admin','editor'].includes(role))return{ok:false,status:403,error:'Účet nemá oprávnění spouštět automatizaci.'};return{ok:true,actor:`${role}:${data.user.id}`};}
async function invoke(name:string){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),120000);try{const response=await fetch(`${SUPABASE_URL}/functions/v1/${name}`,{method:'POST',headers:{authorization:`Bearer ${SERVICE_ROLE_KEY}`,apikey:SERVICE_ROLE_KEY,'content-type':'application/json'},body:'{}',signal:controller.signal});const text=await response.text();let result:any={};try{result=text?JSON.parse(text):{};}catch{}if(!response.ok)throw new Error(`${name} HTTP ${response.status}: ${text.slice(0,800)}`);if(result?.error)throw new Error(`${name}: ${String(result.error)}`);if(result?.ok===false)throw new Error(`${name} oznámil neúspěšný běh.`);return result;}finally{clearTimeout(timer);}}
function pragueToday(){const parts=new Intl.DateTimeFormat('en-GB',{timeZone:'Europe/Prague',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date());const values=Object.fromEntries(parts.map(part=>[part.type,part.value]));return`${values.year}-${values.month}-${values.day}`;}
async function currentImportCount(sourceId:string){const today=pragueToday();const{count,error}=await db.from('leaflet_imports').select('id',{count:'exact',head:true}).eq('source_id',sourceId).in('status',ACTIVE_IMPORT_STATUSES).or(`detected_valid_to.is.null,detected_valid_to.gte.${today}`);if(error)throw error;return Number(count||0);}async function currentOfferCount(storeId:string){const today=pragueToday();const{count,error}=await db.from('offers').select('id',{count:'exact',head:true}).eq('store_id',storeId).eq('status','published').lte('valid_from',today).gte('valid_to',today);if(error)throw error;return Number(count||0);}async function verifySource(source:any){const slug=String(source.store_slug||'');const imports=await currentImportCount(String(source.source_id||''));if(imports<1)throw new Error(`Zdroj ${slug||source.source_id} po kontrole nemá žádný aktuální import.`);let offers=0;if(PRODUCT_REQUIRED.has(slug)){offers=await currentOfferCount(String(source.store_id||''));if(offers<1)throw new Error(`Zdroj ${slug} po kontrole nemá žádnou aktuální publikovanou nabídku.`);}return{imports,offers};}
function verifyTescoOnline(result:any){const selected=Array.isArray(result?.selected)?result.selected.filter((item:any)=>item&&typeof item.url==='string'&&item.url):[];if(result?.ok!==true||selected.length<1)throw new Error('Tesco specializovaný sync nepotvrdil žádný aktuální online leták.');return{documents:selected.length};}
async function verifyKaufland(){const{data,error}=await db.rpc('verify_kaufland_sync_health');if(error)throw new Error(formatError(error));if(!data?.ok)throw new Error('Kaufland databázová kontrola nepotvrdila úspěch.');return data;}
async function markSourceSuccess(sourceId:string,strategy:string){const now=new Date().toISOString();const{error}=await db.from('leaflet_sources').update({last_error:null,last_checked_at:now,last_success_at:now,last_strategy_used:strategy,last_strategy_success_at:now}).eq('id',sourceId);if(error)throw error;}async function markGenericVerificationSuccess(sourceId:string,strategy:string){const{error}=await db.from('leaflet_sources').update({last_error:null,last_strategy_used:strategy,last_strategy_success_at:new Date().toISOString()}).eq('id',sourceId);if(error)throw error;}async function markSourceFailure(sourceId:string,message:string,touchChecked=true){const update:Record<string,string>={last_error:message.slice(0,1000)};if(touchChecked)update.last_checked_at=new Date().toISOString();await db.from('leaflet_sources').update(update).eq('id',sourceId);}
async function runSpecialized(source:any):Promise<PipelineResult>{const slug=String(source.store_slug||'');const functionName=SPECIALIZED[slug];try{if(!functionName)throw new Error(`Pro obchod ${slug} není specializovaný adaptér.`);if(slug==='kaufland'){const result=await verifyKaufland();await markSourceSuccess(String(source.source_id),'scheduled-products-and-documents-verified');return{store:slug,verified_stores:[slug],adapter:functionName,ok:true,current_imports:Number(result.current_imports||0),current_offers:Number(result.current_offers||0),current_documents:Number(result.current_documents||0),result};}const result=await invoke(functionName);if(slug==='tesco'){const verified=verifyTescoOnline(result);await markSourceSuccess(String(source.source_id),'specialized-online-leaflets-verified');return{store:slug,verified_stores:[slug],adapter:functionName,ok:true,current_imports:0,current_offers:0,current_documents:verified.documents,result};}const verified=await verifySource(source);await markSourceSuccess(String(source.source_id),'specialized-products-and-documents-verified');return{store:slug,verified_stores:[slug],adapter:functionName,ok:true,current_imports:verified.imports,current_offers:verified.offers,result};}catch(error){const message=formatError(error);await markSourceFailure(String(source.source_id),message);return{store:slug,adapter:functionName||'missing',ok:false,error:message};}}
async function runSpecializedInBatches(sources:any[]){const results:PipelineResult[]=[];for(let index=0;index<sources.length;index+=SPECIALIZED_CONCURRENCY){const batch=sources.slice(index,index+SPECIALIZED_CONCURRENCY);const settled=await Promise.allSettled(batch.map(runSpecialized));for(const item of settled)results.push(item.status==='fulfilled'?item.value:{adapter:'unknown',ok:false,error:formatError(item.reason)});}return results;}
async function runGeneric(sources:any[]):Promise<PipelineResult>{const slugs=sources.map((source:any)=>String(source.store_slug||''));const verifiedStores:string[]=[];let imports=0;let offers=0;try{const result=await invoke('discover-leaflets');const failures:string[]=[];for(const source of sources){try{const verified=await verifySource(source);imports+=verified.imports;offers+=verified.offers;verifiedStores.push(String(source.store_slug||''));await markGenericVerificationSuccess(String(source.source_id),'generic-products-and-documents-verified');}catch(error){const message=formatError(error);failures.push(message);await markSourceFailure(String(source.source_id),message,false);}}return{stores:slugs,verified_stores:verifiedStores,adapter:'discover-leaflets',ok:failures.length===0,current_imports:imports,current_offers:offers,result,...(failures.length?{error:failures.join(' | ')}:{})};}catch(error){const message=formatError(error);for(const source of sources)await markSourceFailure(String(source.source_id),message,false);return{stores:slugs,verified_stores:verifiedStores,adapter:'discover-leaflets',ok:false,error:message};}}
async function runJobs(specializedSources:any[],genericSources:any[]){const results=await runSpecializedInBatches(specializedSources);if(genericSources.length)results.push(await runGeneric(genericSources));return results;}function successfulStores(results:PipelineResult[]){return[...new Set(results.flatMap(result=>result.verified_stores||[]).filter(Boolean))];}
Deno.serve(async(request)=>{if(request.method==='OPTIONS')return new Response('ok',{headers:CORS});if(request.method!=='POST')return json({error:'Method not allowed'},405);const authorization=await authorize(request);if(!authorization.ok)return json({error:authorization.error},authorization.status);try{const body=await request.json().catch(()=>({}));const requestedSlug=String(body.store_slug||'').trim().toLowerCase();let query=db.from('leaflet_source_pipeline_status').select('*').eq('is_active',true);if(requestedSlug)query=query.eq('store_slug',requestedSlug);const{data,error}=await query.order('store_slug');if(error)return json({error:error.message},500);const sources=data||[];if(!sources.length)return json({ok:true,queued:false,sources:0,triggered_by:authorization.actor,message:'Nebyly nalezeny aktivní zdroje.'});const specializedSources=sources.filter((source:any)=>SPECIALIZED[String(source.store_slug||'')]);const genericSources=sources.filter((source:any)=>!SPECIALIZED[String(source.store_slug||'')]);if(requestedSlug){const results=await runJobs(specializedSources,genericSources);const success=results.every(result=>result.ok);return json({ok:success,queued:false,sources:sources.length,triggered_by:authorization.actor,verified_stores:successfulStores(results),results},success?200:502);}const work=runJobs(specializedSources,genericSources).then(results=>{const failed=results.filter(result=>!result.ok);if(failed.length)console.error('Pipeline source failures',failed);else console.log('Pipeline completed',{verifiedStores:successfulStores(results)});}).catch(error=>console.error('Background pipeline error',formatError(error)));const runtime=(globalThis as any).EdgeRuntime;if(runtime&&typeof runtime.waitUntil==='function')runtime.waitUntil(work);else void work;return json({ok:true,queued:true,sources:sources.length,stores:sources.map((source:any)=>source.store_slug),specialized_runs:specializedSources.length,generic_runs:genericSources.length?1:0,concurrency:SPECIALIZED_CONCURRENCY,triggered_by:authorization.actor,message:'Kontroly byly spuštěny. Každý produktový zdroj musí znovu potvrdit aktuální publikované nabídky.'});}catch(error){return json({error:formatError(error),code:'PIPELINE_REQUEST_FAILED'},500);}});
