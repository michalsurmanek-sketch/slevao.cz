# This internal maintenance function is invoked by a service-role-only DB RPC using x-cron-secret.
# The function performs its own service-role / cron-secret / admin authorization.
verify_jwt = false
