name: Deploy Intersport parser

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/deploy-intersport.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Reclaim obsolete debug slot
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions delete debug-makro-source --project-ref uhampjdqjxmbhaptgitn --yes || true
      - name: Deploy corrected Intersport parser
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy sync-intersport-products --project-ref uhampjdqjxmbhaptgitn --use-api --no-verify-jwt
