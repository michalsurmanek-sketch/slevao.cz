name: Deploy Takko sync (temporary)
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/deploy-takko-temp.yml
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Replace Takko Edge Function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          supabase functions delete sync-takko-products --project-ref uhampjdqjxmbhaptgitn --yes || true
          supabase functions deploy sync-takko-products --project-ref uhampjdqjxmbhaptgitn --use-api --no-verify-jwt
